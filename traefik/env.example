# Copy to .env beside docker-compose.yml on the VPS.

ACME_EMAIL=you@example.com

# Route 53 DNS challenge (standard AWS SDK env vars).
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
# Optional when credentials could touch multiple hosted zones:
# AWS_HOSTED_ZONE_ID=
